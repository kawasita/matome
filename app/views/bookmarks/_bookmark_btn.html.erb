<% if book.bookmarked_by?(current_user) %>
  <%= link_to "ブックマーク解除", book_bookmarks_path(book), method: :delete, remote: true, class: 'btn btn-danger' %>
<% else %>
  <%= link_to "ブックマークする", book_bookmarks_path(book), method: :post, remote: true, class: 'btn btn-info' %>
<% end %>